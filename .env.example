# Copy this to .env and fill values (keep .env out of git).
# Optional: Finnhub token for earnings calendar (free tier works)
FINNHUB_TOKEN=

# Runtime region/timezone (we’ll default to America/Chicago if empty)
APP_TZ=America/Chicago
